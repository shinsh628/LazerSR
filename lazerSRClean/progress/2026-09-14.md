# 2026-09-14 — 퍼포먼스 dan 파이프라인 마무리: rate-vibro + 프로필 위젯 + 레거시 백필 + v7.0.1

`2026-09-11` 세션에서 시작한 퍼포먼스 dan per-play 파이프라인의 남은 항목을 전부 마무리. v6.15.0 →
v7.0.1로 4개 버전 릴리즈.

## v6.15.0 (chore) — 2026-09-11 세션 릴리즈 마무리
- 2026-09-11 작업(로컬 per-play 계산 + Hook 글루 + 서버 저장) 버전 태그만 올려 배포.

## v6.15.1 — dan 관전 오염 방지 + rate-vibro 게이트 연결
- ~~버그 수정~~ `DanPlayCollectorPatch`: `SpectatorPlayer`가 `Player.ImportScore`를 override 없이
  상속해서 관전 중인 남의 스코어로도 Postfix가 발화 → 소유권 검사 없이 그 사람 이름으로 서버
  업로드되던 버그. `ReplayCollectService` 트리거1과 동일한 `RealmUser.OnlineID` 검사 추가. 서버
  `dan_plays`에 새어든 6건은 운영 조치로 별도 정리(코드와 무관).
- ~~신규~~ `RateVibroChecker`(`LazerSR.DanCalculator\PlayerRating\Vibro\`) — mania-hub
  `shouldCheckRateVibro`/`chartVibroAtRate` 포팅. `DanPlayRecordBuilder`/`DanResultRow`에 연결 —
  업로드 레코드에 완전미인정/부분감점/증거예외 3단계 판정 + 결과창 퍼포먼스 dan 행에 VIBRO 표시.

## v7.0.0 — 프로필 위젯(4K dan) + player-skills 파이프 경로
- ~~신규~~ `DanProfileWidget`(선곡 화면 스킨 위젯) — 전체(RC 헤드라인) | 잭/테크/스피드/스태미나 |
  LN(헤드라인) 6칸, 단 이미지(+/-/++/--) + raw 숫자. `LoadComplete` 1회 조회, 서버 on-write 캐시
  읽기 전용.
- ~~신규~~ 런처 `psreq`/`psreqok`/`psreqerr` 파이프 경로 — `GET /api/v1/player-skills/<user>` 대행
  조회(§23 리더보드와 동일 패턴, Hook은 네트워크 금지).
- 서버 `dan_plays` 버킷별 windowed 집계 API(`player-skills.ts` `collectDanClears` 이후 전체 포팅)
  실기 왕복 확인 완료(nginx 로그).

## v7.0.1 — 프로필 위젯 재진입 버그 수정
- ~~버그 수정~~ 결과창→선곡화면 복귀 시 위젯이 자동 갱신될 거라 가정했으나 틀림(선곡화면은
  서스펜드→재개일 뿐 `LoadComplete` 재발화 안 함).
- ~~신규~~ `SongSelectEntryPatch` + `SongSelectEntryState` — `SongSelect.OnEntering`/`OnResuming`
  Postfix로 `EnteredToken`(`Bindable<int>`) bump, 폴링 없이 재진입 신호. `DanProfileWidget`이 필드로
  구독해 재조회.

## 레거시 20K 리플레이 백필 (완료)
- ~~신규~~ 헤드리스 콘솔 `temp\dan-backfill`(osu.Game 비의존, DB 판정 요약값 + 다운로드된 `.osu`
  텍스트만으로 `PlayUploadRecordBuilder.Build` 직접 호출).
- 19,127건 스코어 중 18,942건 처리(185건 채보 파일 없음, 실패 0) → `records.jsonl` 18,577 레코드.
- 서버 `scripts\bulk_insert_dan_plays.py`(1회성, 재계산 없이 순수 INSERT)로 `dan_plays`에 적재.
- 사용자 승인 하에 `dan_verdicts` 집계 재계산까지 서버에서 실행 완료 — `player-skills` API에 백필
  데이터 반영됨.
- **버그 발견**: 백필 속도 최적화로 `Parallel.ForEach` 병렬 처리 시도 → 스모크 25건 재검증에서
  9건 불일치. `DanClassifier` 내부에 스레드 세이프하지 않은 공유 상태 존재(정확한 위치 미조사).
  순차 실행으로 되돌림, 채보/배속 단위 캐싱만 유지.

## 문서
- `docs\guides\architecture.md` §25 갱신 — 퍼포먼스 dan 항목을 "진행 중"에서 "완료"로, 위 4개
  버전의 내용 전부 반영.
- `docs\INDEX.md` 갱신 — `RateVibroChecker.cs`, `DanProfileWidget.cs`, `SongSelectEntryPatch.cs`/
  `SongSelectEntryState.cs` 행 추가.

## 남은 것 (낮은 우선순위)
- 6/7K 버킷 경로 미검증(MSD 불필요, 리스크 낮음).
- Invert(IN) 모드는 백필 스코프 제외 — 필요해지면 별도 작업.
