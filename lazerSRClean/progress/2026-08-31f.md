# 2026-08-31 (6차) — 리플레이 서버 연결 기능 전체 되돌림

키를 없애고도 "0개 업로드, 29개 실패"가 계속돼 원인을 더 파는 대신, 사용자 지시로 **리플레이
동기화 기능 전체를 시작 전(90bc47d, v6.7.0)으로 되돌리고 처음부터 다시 설계하기로 함.**

- `git revert --no-commit a6a2591^..a61cc41` — v6.8.0~v6.9.0의 5개 커밋을 하나의 revert
  커밋으로 되돌림(`37ace2c`). `git diff 90bc47d HEAD`가 빈 결과 - 코드가 정확히 그 시점과
  동일함을 확인.
- 빌드 확인: 오류 0개.
- 태그(v6.8.0~v6.9.0)와 GitHub Release는 기록으로 남겨둠(되돌리기 방식으로 히스토리 보존을
  선택했으므로 삭제하지 않음).
- 삭제된 것: `HookRuntimeContext.cs`, `ReplayUpload/`, `Launcher/Upload/`, 관련 progress 로그
  5개, `docs/guides/architecture.md` §22, `docs/INDEX.md`/`docs/guides/safety.md`의 관련 서술.

## 릴리즈 v6.9.1 (기능 원복, 배포판만 새 버전)

- `.iss`/`.csproj`는 revert로 6.7.0으로 돌아갔으나, 자동 업데이트 비교가 버전 숫자만 보므로
  6.9.0을 쓰던 유저가 "업데이트"로 인식하도록 6.9.1로 새로 매김(내용은 6.7.0과 동일).

## 다음

리플레이 서버 연동은 새 세션에서 설계부터 다시 시작. 서버 쪽(`C:\dev\LazerSrReplayServer`)은
건드리지 않았음 - 다음 설계에서 그대로 쓸지 다시 정할 것.
