# 2026-09-15 — Mapperatorinator 조사 + lazerSR 이식 방향 논의 (코드 변경 없음)

## 참고 레포 clone
- ~~clone~~ `https://github.com/OliBomby/Mapperatorinator` → `C:\dev\lazerSR\temp\Mapperatorinator` (shallow, `--depth 1`). 코드 변경/빌드 연결 없음, 순수 조사용.

## 조사 내용
- 오디오 → osu! 비트맵 생성 AI(Whisper 변형 219M 트랜스포머 + 좌표 정제 diffusion 모델). 4개 게임모드(mania 포함) 지원, MaiMod(AI 모딩 보조)·매퍼 분류기·리듬 복잡도 추정(RComplexion) 등 부속 기능 존재.
- 무게: 추론 자체는 비교적 가벼움(219M, CPU/MPS 폴백 있음, Colab 무료 GPU로도 데모 가능) — 단 학습은 별개로 매우 무거움(5700 GPU시간).
- "서버" 모드(`InferenceServer`/`InferenceClient`)가 코드에 있으나 **로컬 IPC(named pipe) 전용**이지 네트워크 API가 아님 — 실제 API 서비스로 쓰려면 FastAPI 등으로 직접 감싸야 함.

## lazerSR 이식 방향 논의 (결론 아님, 다음 세션에 이어감)
- Python 모델을 Hook 프로세스에 절대 넣지 않고 별도 프로세스로 분리, **Launcher가 통신**(Hook의 네트워크 금지 레드라인 회피). 파이프 프로토콜은 기존 패턴 복제 모드(`architecture.md` §19, 외부 프로그램 → 파이프 → Hook 주입)를 그대로 재사용하는 안이 유력.
- 생성된 맵은 realm import 없이 무한 트레이닝(`InfiniteTrainingBeatmap`)처럼 메모리에서 직접 구성 + `ILocalOnlyPlayerLoader` 파생 화면으로 재생.
- 모델 실행 위치(로컬 번들 vs 원격 서버 호출)가 핵심 트레이드오프 — 로컬 번들은 설치 용량 급증 + GPU 의존, 원격 서버는 운영비 + 오디오 업로드 프라이버시. 초기엔 원격 서버 쪽이 더 현실적이라는 의견 제시함.

## 다음에 할 일
- 위 방향 중 하나 확정 후 파이프 프로토콜/화면 상태머신 구체화.

---

# dan 인포 위젯 — Roxy 패턴축 요약 (v7.1.0)

- ~~조사~~ mania-hub 패턴 클러스터링(~20종, `Patterns\`)과 Roxy 7축(speed/handStream/jack/chordjack/tech/stamina/course)이 코드상 연결이 없는 완전 별개 시스템임을 확인 — 둘을 잇는 매핑은 mania-hub 원본에도 없음.
- ~~설계~~ 시간 기반 패턴-구간 결합 없이, Roxy가 최종 숫자를 만드는 데 실제로 쓰는 7축 구조 신호 자체를 그대로 노출하는 방향으로 스코프 확정 (4K RC, Roxy가 실제로 라우팅을 이긴 경우에만).
- ~~구현~~ `RoxyEstimator.BuildAxisBreakdown` 추가 — 축별 `StreamWeights × StreamSummaries.Aggregate` 비중(`Share`)과, 그 축만 떼어 기존 log압축+선형매핑+isotonic 변환을 통과시킨 근사 `LocalRawDan`을 계산해 `Debug["axisBreakdown"]`에 실음(계산 로직 자체는 미수정).
- ~~구현~~ `ChartClassifier`가 `mixed.NumericDifficultyHint == "roxy-meta-ridge-v3"`일 때만(Sunny 저단 리라우트 시 `ActualEstimatorAlgorithm`이 stale하게 "Roxy"로 남는 함정 회피) `Debug`에서 axisBreakdown을 읽어 `ChartClassification.RoxyAxes`로 노출.
- ~~구현~~ `DanInfoWidget` 둘째 줄에 상위 3개 축 표시(스피드/핸스/미니잭/코드잭/테크/밀도/체력으로 리네임), 최소컷(raw≥2 또는 비중≤5% 제외) 슬라이더 2개를 `[SettingSource]`로 스킨 에디터에 노출. vibro 표시(윗줄 `⚠VIBRO`)는 기존 그대로 — vibro면 축 요약 안 띄움.
- ~~검증~~ `LazerSR.Hook` Release 빌드 통과(경고만, 신규 에러 없음). 실제 맵으로 화면 육안 확인은 미실시.
- ~~배포~~ `.iss`/`csproj` 버전 7.1.0으로 올려 태그 push, GitHub Actions 릴리즈.

## v7.1.1 — 축 비중 정규화 버그 수정 (실기 확인 후 당일 핫픽스)

- ~~실기 리포트~~ 어려운 맵에서만 밀도/체력 둘 중 하나만 뜨고, 그 외엔 패턴이 아예 안 뜬다는 사용자
  보고.
- ~~조사~~ `dev\OsuScoreModel` 예제 `.osu` 9000개 중 4K Roxy 적중 418개로 스모크 테스트 — naive 정규화가
  stamina/course 두 축에 평균 96%를 몰아준다는 걸 확인(나머지 5축 합쳐 4% 미만). 원인: stamina/course의
  감쇠 시간상수가 나머지 5축보다 10~170배 길어서 raw 누적값 자체가 맵 내용과 무관하게 항상 압도적으로
  큼.
- ~~시도 후 폐기~~ 순수 수학(tau 비율) 정규화 — 오히려 stamina가 418/418 전부 1등(staminaIn 공식 안의
  중첩 누적기 `maxHandStamina`를 tau 비율만으론 못 잡음). 합성 채보로 축별 이론 한계치를 시뮬레이션하는
  것도 시도했으나 Roxy 내부 구조적 보정 로직 때문에 신뢰 가능한 기준값을 못 얻음.
- ~~채택~~ 418개 실측 맵의 축별 raw 기여값 **중앙값**을 코드 상수(`AxisShareReference`)로 박아 각 축을
  자기 스케일로 나눈 뒤 비중 계산 — 평균 비중 0.07~0.20으로 고르게 분산, 맵마다 1등 축이 달라짐을 재검증.
- ~~구현~~ `RoxyEstimator.BuildAxisBreakdown`의 `Share` 계산만 수정(`LocalRawDan`은 실제 dan 스케일 유지
  위해 그대로 raw 기여값 사용). 스모크 테스트 하네스는 스크래치패드에서 작업 후 삭제.
- ~~검증~~ `LazerSR.Hook` Release 빌드 통과.
- ~~배포~~ v7.1.1 태그 push, GitHub Actions 릴리즈.

## v7.2.0 — 7축 방식 폐기, 패턴+난이도 시계열 결합 방식으로 교체

- ~~조사~~ mania-hub 및 Roxy 원저작자(LeoBlackMT, GitHub `osumania_map_analyser`) 공개 저장소 확인 —
  7축을 사용자에게 노출하는 선례가 아예 없고, 원저작자 본인의 유일한 시도(`RoxyHandSplitTech`)도
  "과적합이라 폐기"로 디버그 아카이브에 묻혀 있음을 확인. 7축 방식 폐기 결정.
- ~~설계~~ 패턴 분류(LeoBlack 20여종 클러스터링, 원문 이름 그대로)와 난이도(Roxy 400ms 구간 곡선)를
  별개 파이프라인으로 얻어 시간으로 이어붙이는 방식으로 재설계 — 서로 다른 축끼리 비교하지 않으므로
  7축 방식이 겪은 스케일 불일치 문제가 구조적으로 없음.
- ~~구현~~ `PatternService.FindPatternWindows` 추가(클러스터링 전 원시 매치 리스트 노출, 기존 파이프라인
  미변경). `RoxyEstimator.BuildSectionCurve` 추가(`ComputeSectionAggregate`가 스칼라로 접기 직전의
  400ms 구간별 값 추출, 계산 로직 미변경). `ChartClassifier.BuildRoxyPatternDifficulty` 신규 — Roxy의
  캐노니컬 시간축을 원본 `.osu` 시각으로 역변환해 두 데이터를 겹쳐 패턴별 시간비율(`TimeShare`)과 맵
  자체 피크 대비 상대강도(`RelativeIntensity`)를 산출.
  `ChartClassification.RoxyPatterns`로 노출(기존 `RoxyAxes`는 필드만 유지, 위젯은 더 이상 안 씀).
- ~~구현~~ `DanInfoWidget` 둘째 줄을 패턴 요약으로 교체(`RoxyAxisRawFloor`/`ShareFloorPercent` 슬라이더 →
  `PatternShareFloorPercent`(기본 5%)/`PatternIntensityFloorPercent`(기본 50%)로 교체). 이름은 원문
  그대로(사용자 확정).
- ~~검증~~ `dev\OsuScoreModel` 무작위 4K 샘플로 `ChartClassifier.ClassifyChart`를 직접 호출해 Roxy 적중
  맵 10개 육안 확인 — 맵마다 서로 다른 패턴 조합이 자연스럽게 뜨고 특정 항목 독점 현상 없음
  (예: "Chordstream · Handstream · Jumpstream", "Longjacks · Chordjacks", "Stream · Minitrills ·
  Rolls"). 스모크 테스트 하네스는 스크래치패드에서 작업 후 삭제.
- ~~빌드~~ `LazerSR.Hook` Release 빌드 통과.
- ~~배포~~ v7.2.0 태그 push, GitHub Actions 릴리즈.
