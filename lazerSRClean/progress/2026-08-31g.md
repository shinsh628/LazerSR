# 2026-08-31 (7차) — 리플레이 저장 서버 연동 재구현 (키 없음)

되돌렸던 v6.8.0~v6.9.0을 설계부터 다시. 서버(`C:\dev\LazerSrReplayServer`)도 같은 세션에서 고침.

## 서버 (별도 저장소)

- ~~진단~~ "0개 업로드, 29개 실패"의 원인 = `POST /api/v1/replays` 500 `no such table: scores`.
  워커가 재시작 없이 옛 상태로 떠 있어 `init_db()`가 산 DB에 안 돌았음. 스키마 생성 + `systemctl
  restart` + curl 왕복 검증(201/200 duplicate/.osr 기록) + probe 삭제 → DB 다시 빈 상태
- ~~생성~~ `GET /api/v1/stats` → `{"replay_count": N}` (런처 라벨용)
- ~~생성~~ `deploy/push.sh` (scp + chown + **restart** + 검증). 서버엔 git 없음, 로컬이 원본

## Hook

- ~~생성~~ `ReplayUpload\HookRuntimeContext.cs` — 파이프 트리거 벌크 스캔용 Realm/Storage/Api 공유
  (`DifficultyDisplayPatch`가 선곡 화면에서 채움)
- ~~생성~~ `ReplayUpload\ReplayQueueWriter.cs` — 큐 엔트리 작성 + `.osr` 경로 해결 + `ClearQueue`.
  두 트리거 공용. 모드·정확도·성공 여부 안 가림 (mania만)
- ~~생성~~ `ReplayUpload\ReplayCollectService.cs` — 트리거 1(realm 벌크 스캔). 2중 소유권 검사:
  `RealmUser.OnlineID == 로그인 id (>1)` **AND** `.osr` 헤더 플레이어 이름 == 로그인 유저네임
- ~~생성~~ `ReplayUpload\OsrHeader.cs` — `.osr` 헤더 플레이어 이름 파서 (파싱 실패 = 제외)
- ~~생성~~ `Patches\ReplayAutoUploadPatch.cs` — 트리거 2. `Player.ImportScore` Postfix
  (`PersonalSunnyScoreCollectorPatch`와 동일 논리 → 훈련/구간연습/리플레이 자동 제외).
  `__result` Task 대기 후 `.osr` 디스크 기록까지 150ms×20 재시도
- ~~수정~~ `Ipc\PipeServer.cs` — `replaycollect:scan` 핸들러 → `replaycollect:queued:N`/`notready`/`error` 회신
- ~~수정~~ `Patches\DifficultyDisplayPatch.cs` — `HookRuntimeContext.Populate` 2줄 추가

## Launcher

- ~~생성~~ `Replay\ReplayServerClient.cs` — 큐 드레인 + multipart 업로드 + `/stats` 조회.
  **실패 시 HTTP 상태코드 + 응답 본문 그대로 노출** (예전엔 `catch{return false}`로 삼켜서 500을 못 봄)
- ~~수정~~ `MainWindow.xaml(.cs)` — "리플레이 수집" 버튼 + "리플레이 N개 저장됨" 라벨,
  큐 폴더 `FileSystemWatcher`(트리거 2 자동 업로드, 1.5s 디바운스) + 시작 시 잔여 큐 드레인,
  `PipeClient.MessageReceived` 구독 복원. 창 높이 200→262
- ~~수정~~ `.csproj`/`.iss` 버전 6.9.1 → **6.10.0**

## 빌드/배포

- ~~확인~~ `dotnet build LazerSR.Launcher -c Release` 오류 0, 새 경고 0
- 태그 `v6.10.0` push → GitHub Actions 릴리즈

## 미검증 (다음 세션 / 실사용 QA)

- osu! 안에서의 실제 동작: Harmony 패치 발동, `HookRuntimeContext` 채워지는 타이밍, realm 스캔 결과.
  빌드·정적 검증만 됨 — 런처로 osu! 띄워서 수동 확인 필요
- `.osr` 헤더 파서는 합성 파일로만 검증 (`byte·int32·string·string` 포맷)
