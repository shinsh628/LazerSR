# 2026-09-01 — 리더보드 모드 세부설정: 서버 사이드 (응답 + 백필)

리더보드 탭에서 DT 1.2배속 기록이 깡 DT로 보이는 문제. 3계층(업로드/서버저장/서버응답/클라복원)
전부 acronym만 취급하는 게 원인. 이번 세션은 **서버 사이드만** 처리.

## 조사 (수정 안 함)

- `ReplayQueueWriter.cs:84` `mods = score.APIMods.Select(m => new { acronym = m.Acronym })` → `m.Settings` 버림
- `leaderboard.py` `_mod_acronyms()` → `"mods": sorted(acronyms)` 문자열 배열로 평탄화
- `LazerSrScoreFactory.cs:104` `m.GetString()` → `CreateModFromAcronym` (기본값 모드), 서버 `rate` 필드도 무시
- 바닐라 osu는 `SoloScoreInfo.mods` = `APIMod[]`(`{acronym, settings}`) → `ToMod()` → `ModIcon`(확장정보 기본 ON)
  → `ModRateAdjust.ExtendedIconInformation`이 `1.20x` 플레이트 그림. **표시 코드는 손댈 게 없다.**
- lazer `.osr`(version ≥ 30000001) 꼬리에 LZMA JSON `LegacyReplaySoloScoreInfo.mods = APIMod[]`(설정 포함) 존재.
  stable 임포트(version < 30000000)엔 없고 원래 세부설정 개념도 없었음 → 깡 모드가 정답.

## 서버 (`C:\dev\LazerSrReplayServer`)

- ~~수정~~ `src/api/leaderboard.py` — 응답 행에 `mods_detailed`(APIMod 원형) 추가. `_mods_detailed()` 헬퍼.
  기존 `mods`(정렬 acronym 문자열)는 **구 클라 v6.11.2 호환 위해 유지** — 그 클라 팩토리가 객체를 받으면
  `m.GetString()`에서 행 전체가 누락되므로 additive로만.
- ~~생성~~ `scripts/backfill_mods.py` — `.osr` 헤더 파서 + LZMA 꼬리 디코드 → `scores.mods` UPDATE.
  `--dry-run`/`--apply`. 트레일러 파싱 로직 레퍼런스로 보존.
- ~~실행~~ DB 백업(`replay.db.bak-20260831-mods`) → stop → `--apply` → start.
  16,300행: lazer 13,773 / stable 2,527 스킵 / 오류 0. **`mods` 1,177행 갱신**(DT/HT/DA 세부설정),
  `rate`는 이미 정확(0행).
- ~~배포~~ `deploy/push.sh` (leaderboard.py). ping/stats 정상.
- ~~검증~~ `curl ?mods=DT` → `mods_detailed:[{acronym:DT, settings:{speed_change:1.2, adjust_pitch:true}}]`

## 남은 작업 (클라, 다음 세션)

1. `ReplayQueueWriter.cs:84` — 업로드 페이로드에 `settings = m.Settings.Count>0 ? m.Settings : null` 추가
2. `LazerSrScoreFactory.cs:99-110` — `mods_detailed` 우선 파싱 → `APIMod{Acronym,Settings}` 조립 →
   `apiMod.ToMod(mania)`. `JsonElement` 값 언랩 헬퍼 필요. 없으면 문자열 `mods` fallback(하위호환)
3. 버전 올리고 태그 (v6.11.3)
4. `ReplayQueueWriter`의 `rate`/`GetSpeedChange` 정리 여부는 사용자 결정 대기

## 알아둘 것

- `scores.passed` 전 행 `1` — 트리거가 실패판도 올리는데 값 안 채워짐. rank F/D도 passed=true로 리더보드 노출.
- DB 실측: scores 16,300 (popolee 11,967 / osumania 4,310 / shinsh628 23), beatmaps 3,313.
