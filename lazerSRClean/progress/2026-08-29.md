# 2026-08-29

## 실시간 sunny 위젯

- ~~`Widgets/RealtimeSunnyWidget.cs`~~ 게임플레이 HUD 스킨 위젯. 기본 sunny pill과 같은 생김새
  (`StarRatingDisplay`), 앞으로 400ms 구간의 난이도를 100ms마다 갱신.
  - 로딩 중 `SunnyManiaDifficultyCalculator.GetStrainTimeline` 1회 사전계산 → 이후 배열 조회만
  - 매 틱: `현재시각 ~ min(+400ms, 맵 끝)` 윈도우의 모든 노트 strain 산술평균(노트 0이면 0)
    - 초기안은 상위 4개 평균이었으나 로컬 피크만 반영돼 맵 전체 sunny보다 과하게 높아 산술평균으로 교체
  - 합산: 상위/중위 퍼센타일·파워민 없이 raw를 짧은맵보정 이후 파이프라인만 통과 —
    `SunnyTempNerf.Apply(raw) × 0.975`
  - 윈도우 폭은 배속 무관 고정 400ms(비트맵 시간). 동시치기는 원본처럼 값 중복 그대로
  - sunny/sunny+ 구분 안 함 — 런처 체크박스가 정하는 프로세스 전역 기본값을 그대로 읽음
  - 개인화 diff는 위젯 설정 토글(`UsePersonalSunny`)로 opt-in — 켜면 `WithIsolatedDiff`로 베이크,
    토글 변경 시 재베이크
- ~~`Patches/SkinWidgetRegistrarPatch.cs`~~ 위젯 등록 1줄
- ~~`docs/guides/architecture.md`~~ §20 신설
- ~~`docs/INDEX.md`~~ Widgets 행에 추가

## 빌드

- 로컬 Release 빌드만. GitHub push / 태그 / CI 릴리즈 없음.
