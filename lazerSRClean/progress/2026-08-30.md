# 2026-08-30

## 런처 자동 업데이트

- ~~`LazerSR.Launcher/Update/UpdateChecker.cs`~~ 신규. GitHub Releases(`shinsh628/LazerSR`, public이라
  토큰 불필요) `releases/latest` 조회 → 새 버전이면 setup exe를 `%TEMP%`에 다운로드하고 실행.
  진행률 창은 코드로 생성(별도 xaml 없음). 모든 실패는 null/false로만 알림 — 런처 실행 안 막음.
- ~~`LazerSR.Launcher/App.xaml`~~ `StartupUri` 제거.
- ~~`LazerSR.Launcher/App.xaml.cs`~~ `OnStartup`에서 GUI 전에 검사 → `MessageBox`(예/아니오).
  예=다운로드 후 `Environment.Exit`, 나중에/실패/없음=`MainWindow` 표시.
- ~~`LazerSR.Launcher/LazerSR.Launcher.csproj`~~ `<Version>` 추가 (로컬 폴백용).
- ~~`.github/workflows/build-installer.yml`~~ launcher publish에 `/p:Version=<태그버전>` 주입.
- ~~`docs/guides/architecture.md`~~ §21 신설. ~~`docs/INDEX.md`~~ `Update/` 행. ~~`docs/end.md`~~ 버전 주입 설명.

## 릴리즈 v6.7.0

- `.iss` `MyAppVersion` 6.6.2→6.7.0, `.csproj` `<Version>` →6.7.0.
- master 커밋(82b77f9) + push, `v6.7.0` 태그 push → GitHub Actions `build-installer.yml`
  success → Release v6.7.0 게시(`LazerSR-v6.7.0-Setup.exe`). 첫 자동 업데이트 대상 릴리즈.
- (2026-08-31) `end.md` 1a에 버전 주입/버전 bump 절차 반영, `safety.md` 레드라인 2에 런처 예외 명시.
