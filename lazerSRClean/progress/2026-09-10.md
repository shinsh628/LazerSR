# 2026-09-10 — mania-hub dan classifier C# 포팅 (진행 중, 미완)

**세션 사유**: 곡 선택화면 dan 뷰어를 sunny→임계 테이블 방식에서 **mania-hub의 dan 분류 파이프라인**(= 벤더링된 LeoBlack 엔진)으로 교체. 4/6/7K 지원. 최종 목표는 판 종료 시 리플레이와 함께 (mod 적용) 차트 dan + 퍼포먼스 dan을 서버로 전송하고, 서버가 mania-hub의 정확도별 dan 곡선을 API로 제공하는 것.

## 결정 (사용자)

- **sunny는 우리 것 유지** — LeoBlack sunny로 교체 안 함. 실측: 우리 `SunnyManiaDifficultyCalculator`(바닐라) vs LeoBlack `sunnyAlgorithm.js`가 **≥4★에서 mean|Δ| 0.037로 일치**, <3★에서만 발산(LeoBlack이 저SR을 +0.3~0.9 들어올림). sunny+ 개인화 시스템이 우리 sunny에 용접돼 있어 교체 시 그것까지 재구축해야 함 → 유지.
- 그 외 **전부 포팅** (Roxy/Azusa/Daniel/Companella ONNX/Mixed/patterns/interlude/vibro/LN kNN/dan-eligibility/invert-mod/benchmark scoring/dan-credit 곡선).
- dan 코스 레지스트리는 **범위 밖** (안 함).
- 서버 저장은 mania-hub `dan_estimates` 스키마 그대로. 세부는 나중 논의.

## 완료 — `LazerSR.DanCalculator` 신규 프로젝트 (~69파일, ~23,300줄, 빌드 그린 0에러)

`C:\dev\lazerSR\lazerSRClean\LazerSR.DanCalculator\` — `LazerSR.SunnyCalculator`와 동급 프로젝트. Hook이 참조하게 될 것(아직 미연결).

| 디렉터리 | 내용 | 출처 |
|---|---|---|
| `Beatmap/` | ManiaBeatmap 파서 | mania-hub `beatmap-parser.ts` |
| `Features/` | DanMath, DanFeatures, DanPatterns, MotionFeatures, JackDemand, NoteBpm | mania-hub `dan-estimator/` |
| `Types/` | DanTypes (DanFeatureMetrics 51필드, DanEstimate 등) | `types.ts` |
| `Parser/` | OsuFileParser, PatternOsuParser, NoteColumn | LeoBlack `js/parser/` |
| `Intervals/` | 4/6/7K rc·ln 테이블 + DanIndex | LeoBlack `js/estimator/intervals/` (mania-hub 벤더판 = 6k-rc Terra~Finish 확장 포함) |
| `Interlude/` | Interlude SR (Companella 입력) | LeoBlack `js/interlude/` (11파일) |
| `Patterns/` | 패턴 클러스터/카테고리 | LeoBlack `js/patterns/` (9파일) |
| `Estimators/` | ReworkMathCore, ReworkEstimatorUtils(estDiff), RcDifficultyFormat, DanielAlgorithm, Azusa, **Roxy**, RoxyMetaModel, MarathonCorrection, **Mixed**, Companella(ONNX), **SunnyShim** | LeoBlack `js/rework/`+`js/estimator/` |
| `Vibro/` | VibroDetection, VibroSections, VibroClearEvidence, LongjackVibro | mania-hub `vibro-*.ts` + LeoBlack `vibro.js` |
| `Classifier/` | ChartClassifier(652), LeoBlackEstimator(파싱), DanEligibility, InvertMod, CacheVersion, Labels, LnDan(kNN+127엔트리 코퍼스), DanTables, Companella 오케스트레이터 | mania-hub `chart-classifier.ts` 등 |
| `Credit/` | DanCredit — 정확도→퍼포먼스 dan 곡선 | mania-hub `dan-credit.ts` |
| `Benchmark/` | Scoring(3159줄), DanEstimator, FamilyChoice, Courses — deprecated `estimateDan` 벤치마크 경로(ClassifyChart에 미연결, parity용) | mania-hub `dan-estimator/` |
| `Msd/` | MinaCalcNative(P/Invoke, osu.Game 제거판), Msd 어댑터 | mania-hub `msd.ts` + Hook `MsdCalculator.cs` |
| `Assets/` | `dan_model.onnx` (Companella 신경망 304KB) | LeoBlack |

- NuGet: `Microsoft.ML.OnnxRuntime` 1.20.1. `MinaCalc.dll`은 Hook에서 링크.
- 진입점: `LazerSR.DanCalculator.DanClassifier.ClassifyChart(osuText, input)` / `.ClassifyChartWithCompanellaAsync(...)`.
- **SunnyShim**: `osu.Game` 경계 (이 1파일만 osu.Game 참조). osuText 디코드 → `SunnyManiaDifficultyCalculator().Calculate(playable, rateMods)` (`WithIsolatedDiff(zero, forceVanillaTail:true)` 바닐라) → star. estDiff는 포팅된 `ReworkEstimatorUtils.EstDiff`. **버그 수정함**: 이미 변환된 ManiaBeatmap을 FlatWorkingBeatmap으로 재감싸 SunnyRunner가 이중변환 → 상수화되던 것을 IBeatmap-직접 경로로 교체.

## 포팅 방식

`LazerSR.DanCalculator\PORTING.md`에 마스터 스펙 (JS→C# 규칙표, 모듈맵, sunny/MSD 치환 계약). 병렬 서브에이전트 다수로 진행. P5/P8은 사용량 한도로 중단→재시도/직접 완료. 최종 통합·빌드 그린화는 P7 에이전트 + 직접.

## 검증 (스모크 테스트, `C:\dev\lazerSR\temp\dan-smoke\`)

`DanClassifier.ClassifyChart` vs LeoBlack 원본 JS `runMixedEstimatorFromText` (node), 4K 랜덤 코퍼스(`OsuScoreModel\temp\2026_06_02_osu_files`, 전부 4K).

- **50/50 예외 없이 분류.**
- **OUR sunny ≥ 4★ (≈ leoStar ≥ 4.5): base-dan 일치 95%, within-1-tier 100%, 편향 +0.04.** leoStar ≥ 5.0: 94%/100%/+0.05.
- <4★ Kyu 구간에서만 발산 — 원인 단일: **`chart-classifier`의 `sunnyLowEndReroute` 가드**(`SUNNY_LOW_END_MAX_STAR = 3.0`)가 우리 sunny 저SR 리드(1~2★ 낮음)에서 LeoBlack이면 발동 안 할 쉬운 차트에도 발동 → "< Intro 1"으로 리라우팅. estimator 로직 자체는 프로브에서 LeoBlack과 numeric 일치 확인(2700232, 4474059).
- **사용자 판단: ≥4★ 일치 확인됐으므로 그대로 수용.** 저단 가드 재보정은 선택(후순위).

## 미완 / 다음 작업

1. **6/7K 실검증** — 코퍼스에 6/7K .osu가 없음. 별도 수집 필요. (6/7K 경로는 순수 sunny→테이블이라 로직 리스크 낮음.)
2. **Companella async 경로** 검증 — sync는 `companellaPending`만 세팅, async(`--async`)에서 ONNX 실행. MSD는 단일 MinaCalc 버전(LeoBlack은 6버전 핀) → tier 1칸 차이 가능. 4K RC <9★만 영향, 6/7K 무관.
3. **dan-credit 곡선** 검증 (정확도→퍼포먼스 dan).
4. **Hook 통합**: `Calculators/DanCalculator.cs`(현행 sunny→임계) 폐기 후 `LazerSR.DanCalculator` 파사드로 교체. `Widgets/DanInfoWidget.cs` RC/LN half·tier·boundary·confidence·vibro 풍부 표시로 개작. `LazerSR.Hook.csproj`에 `LazerSR.DanCalculator` ProjectReference 추가.
5. **스코어 수집 패치**: `Player.ImportScore` Postfix로 판 종료 시 (mod 적용) 차트 dan + 판정카운트 + stable/v2 정확도 계산 → `ReplayUpload/ReplayQueueWriter.cs` 큐 엔트리에 dan 필드 추가.
6. **런처** `Replay/ReplayServerClient.cs` multipart에 dan 필드.
7. **서버(Python, `LazerSrReplayServer`)**: `dan_estimates` 스키마 테이블 + 플레이당 dan 컬럼 + `dan-credit` 곡선 포팅(또는 raw 저장 후 read-time) + 퍼포먼스 dan API 엔드포인트.

## 스텝 4 — Hook 통합 + 배포 (v6.14.0)

- **ONNX 모델 경로 버그 수정**: `CompanellaEstimator`가 `AppContext.BaseDirectory\dan_model.onnx`에서
  로드 → Hook 주입 시 그건 osu! 설치 경로. `ResolveModelPath()` 추가 — `MinaCalcNative`와 동일하게
  어셈블리 위치 우선, `Assets\` 하위 + BaseDirectory 폴백. csproj `<None>`에 `<Link>`로 출력 루트에 복사.
- `LazerSR.Hook.csproj` → `LazerSR.DanCalculator` ProjectReference 추가.
- `LazerSR.Hook\Calculators\DanCalculator.cs`(sunny→임계 20단계) **삭제**. `docs\SunnyToDan.txt`도 삭제(고아).
- `Widgets\DanInfoWidget.cs` 개작 — `LegacyBeatmapEncoder`로 `.osu` 재인코딩 → `DanClassifier.ClassifyChart`
  (sync). RC/LN half·tier·경계·confidence·vibro 2줄 표시. BPM/dominant 꼬리표는 유지. 240×28 → 260×44.
- `.iss [Files]`에 `LazerSR.DanCalculator.dll` 추가, `ExcludeHookDepsFromSingleFile` 목록에도 추가.
- 버전 6.13.1 → **6.14.0** 릴리스(태그 push, CI 성공).

### v6.14.1 — Companella(ONNX) 활성화 + 표시 간소화 (사용자 지시)

- 위젯을 `ClassifyChart`(sync) → **`ClassifyChartWithCompanellaAsync`**로 교체. 매 맵/모드 갱신마다
  Companella 정제(4K RC <9★에서만 MSD+InterludeSR+ONNX 실제 실행, 그 외는 sync 결과 반환).
- **BPM·지배패턴·confidence(%) 표시 전부 제거.** 위젯은 이제 primary dan half + tier + 경계 + vibro만.
  `SunnyState.CurrentDominant`/`PatternBpmCalculator` 소비 안 함(다른 곳은 그대로 씀). 220×40.
- `CompanellaEstimator.InstallNativeResolver()` 추가 — `Microsoft.ML.OnnxRuntime`의 `[DllImport("onnxruntime")]`
  네이티브 로드를 우리 로프 배포로 리다이렉트(`NativeLibrary.SetDllImportResolver`, osu! 프로세스 내 프로브 경로 문제).
- 배포: `dan_model.onnx` + `onnxruntime.dll` + `onnxruntime_providers_shared.dll` +
  `Microsoft.ML.OnnxRuntime.dll` 로프 배포. `.iss [Files]` 4줄 + `ExcludeHookDepsFromSingleFile`
  (dll은 필터, `dan_model.onnx`는 별도 조건으로 단일파일 번들 제외). `DropUnusedOnnxFromPublish` 타깃 삭제.
- 버전 → **6.14.1**.
- 빌드: Hook Release 그린, Launcher publish 그린(5개 로프 파일 확인, 모델 exe 미번들 확인).
- 문서: `architecture.md` §24 갱신, `INDEX.md` 갱신.
- **미착수(스텝 5~)**: 스코어 수집 패치·dan 필드·런처 multipart·서버. 6/7K·Companella·dan-credit 실측.

## temp 작업물 (보존, clear 후 재개용)

- `C:\dev\lazerSR\temp\mania-hub\` — mania-hub 레포 클론 (포팅 source of truth, `live-backend/src/dan/` + `live-backend/vendor/leoblack/`)
- `C:\dev\lazerSR\temp\osumania_map_analyser\` — LeoBlack 원본 레포 (커밋 a36a21b, mania-hub 벤더판과 동일 커밋)
- `C:\dev\lazerSR\temp\dan-smoke\` — 스모크 테스트 하니스 (C# `DanClassifier` runner + `leo-mixed.mjs` node 비교)
- `C:\dev\lazerSR\temp\sunny-compare\` — sunny 실측 하니스 (우리 sunny vs LeoBlack sunny)
