# 2026-08-31 (3차) — 계정 필터 진단 빌드

v6.8.1의 로그인 계정 필터가 실사용에서 거꾸로 동작함(shinsh628 PC에서 눌렀는데 popolee 것만
29건 올라감, shinsh628 본인 것은 0건). 원격 클라이언트 상태를 직접 못 들여다보므로 추측으로
또 고치지 않고 **진단 정보를 노출하는 빌드**를 먼저 냄.

- ~~`ReplayUploadService.EnqueueAllLocalMania()`~~ 반환 타입을 `int?`에서 `SyncDiagnostics`
  record struct(`Written`, `LocalUserId`, `LocalUsername`, `ManiaWithReplaySeen`,
  `DistinctRealmUsersSeen`)로 변경. mania+리플레이 있는 스코어를 다 세면서 `RealmUser.Username(id=...)`
  집합을 모은다.
- ~~`PipeServer.HandleReplayUploadSyncAll`~~ 회신 포맷 확장:
  `replayupload:queued:<N>:localId=<X>:localName=<Y>:seen=<M>:users=[a(id=1),b(id=2),...]`
- ~~`MainWindow.xaml.cs`~~ 이번 빌드는 **실제 업로드를 안 함** — 받은 진단 문자열을 그대로
  StatusTextBlock에 띄우고 멈춘다. 사용자가 그 문자열을 그대로 알려주면 원인 확정 가능.

빌드 확인: 오류 0개.

## 릴리즈 v6.8.2 (진단 전용, 다음 버전에서 실제 수정 예정)

- `.iss`/`.csproj` 6.8.1→6.8.2.
