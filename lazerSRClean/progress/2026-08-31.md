# 2026-08-31

## 리플레이 일괄 동기화

- ~~`LazerSR.Hook/HookRuntimeContext.cs`~~ 신규. `DifficultyDisplayPatch`가 채우는 RealmAccess/Storage
  공유 홀더 — 파이프 트리거 작업이 Drawable 컨텍스트 없이도 접근하도록.
- ~~`LazerSR.Hook/ReplayUpload/ReplayUploadService.cs`~~ 신규. realm 전수 스캔(mania, `.osr` 있는 것만,
  무덤/러브드 포함) → `LazerSrStorage`에 점수당 JSON 큐 파일. 네트워크 없음.
- ~~`LazerSR.Hook/Ipc/PipeServer.cs`~~ `replayupload:syncall` 명령 추가 → 큐 작성 후
  `replayupload:queued:<N>`/`notready`/`error` 회신.
- ~~`LazerSR.Hook/Patches/DifficultyDisplayPatch.cs`~~ RealmAccess/Storage를 `HookRuntimeContext`에 stash.
- ~~`LazerSR.Launcher/Upload/ReplayUploader.cs`~~ 신규. 큐 폴링 → 첫 파일의 `osu_username`으로
  `POST /api/v1/register` 자동 등록(키 없을 때만) → 점수마다 `multipart/form-data` POST → 성공 시
  큐 파일 삭제. `UpdateChecker`와 동일한 방어적 패턴.
- ~~`LazerSR.Launcher/Configuration/LauncherSettings.cs`~~ `ReplayServerApiKey` 필드 추가.
- ~~`LazerSR.Launcher/Configuration/InstallPathProvider.cs`~~ **버그 수정**: `Save()`가 기존 설정을
  통째로 덮어써서 osu! 경로만 바꿔도 `ReplayServerApiKey`가 날아가던 문제 — 기존 설정 로드 후
  `with` 식으로 필드만 갱신하도록 수정.
- ~~`LazerSR.Launcher/MainWindow.xaml`~~ "리플레이 동기화" 버튼 추가(창 높이 200→230).
- ~~`LazerSR.Launcher/MainWindow.xaml.cs`~~ 버튼 클릭 → 파이프로 `syncall` 전송, `MessageReceived`로
  Hook 응답 받아 `ReplayUploader.SyncAsync` 호출.
- 빌드 확인: `dotnet build LazerSR.Launcher -c Release` 오류 0개 (경고는 기존 OFSG001 + 신규 CS4014
  fire-and-forget 1개, 둘 다 무해).
- ~~`docs/guides/architecture.md`~~ §22 신설. ~~`docs/guides/safety.md`~~ 레드라인 2에 리플레이
  업로드도 같은 프로세스-경계 근거로 허용됨을 명시. ~~`docs/INDEX.md`~~ 새 파일/폴더 행 추가.

**서버 쪽**: 별도 프로젝트 `C:\dev\LazerSrReplayServer\`, DigitalOcean(`68.183.226.182`, HTTP, 도메인 없음)에
이미 배포·구동 중. `POST /api/v1/register`(인증 없음, osu_username만) / `POST /api/v1/replays`
(X-Api-Key, score_guid로 idempotent) / `POST·GET /api/v1/presence`(이번엔 미사용) / `GET /api/v1/ping`.

**이번에 명시적으로 뺀 것** (다음에 할 것으로 남김):
- 매 플레이 결과창 진입 시 자동 업로드(`Player.ImportScore` 훅) — 지금은 수동 버튼으로 로컬 realm
  전체를 훑는 방식만 구현. 신규 플레이는 다음 "동기화" 클릭 때까지 안 올라감
- 온라인 상태(presence) 하트비트 — 서버 엔드포인트는 있지만 Launcher가 아직 안 부름
- `calc_version`/sunny SR 재계산 — 로컬 realm에 있는 원본 pp/정확도만 그대로 보냄

## 릴리즈 v6.8.0

- `.iss` `MyAppVersion` 6.7.0→6.8.0, `.csproj` `<Version>` →6.8.0.
