# 2026-08-31 (2차) — 리플레이 동기화 버그 픽스

## 발견된 버그 2건 (실사용 중 발견)

1. **서버**: `POST /api/v1/replays`의 `REQUIRED_FIELDS`에 `calc_version`이 필수로 남아있었음.
   `ReplayUploadService`는 애초 계획대로 `calc_version`을 안 보내므로 업로드 58건 전부
   `400 bad_request`로 거부됨(0개 업로드, 58개 실패). `C:\dev\LazerSrReplayServer`
   (별도 프로젝트, 서버에 직접 배포)에서 `calc_version`을 필수 목록에서 빼고 `meta.get()`으로
   변경 — 없으면 `NULL` 저장.
2. **Hook**: 남의 리플레이를 인게임에서 다운로드해서 보면 그것도 로컬 realm에 `ScoreInfo`로
   남는다는 걸 몰랐음. `Ruleset.OnlineID == 3` 필터만 걸어서 **다른 유저(popolee) 리플레이까지
   그 유저 이름으로 서버에 올라간 채 확인됨**(실제로 `users` 테이블에 `popolee`가 자동 등록돼
   있었음 — shinsh628이 아니라 shinsh628이 구경한 리플레이 소유자 이름).

## 조치

- ~~서버 REQUIRED_FIELDS 수정~~ 배포 완료(이전 세션에서 이미 조치, 이번 세션 시작 시점엔 반영돼 있었음).
- ~~`LazerSR.Hook/HookRuntimeContext.cs`~~ `IAPIProvider Api` 필드 추가.
- ~~`LazerSR.Hook/Patches/DifficultyDisplayPatch.cs`~~ `apiProvider`도 `HookRuntimeContext`에 stash.
- ~~`LazerSR.Hook/ReplayUpload/ReplayUploadService.cs`~~ `score.RealmUser.OnlineID == api.LocalUser.Value.Id`
  필터 추가. 로그인 안 돼 있으면(`localUserId <= 0`) `notready` 처리.
- 빌드 확인: 오류 0개.
- **서버 데이터 전체 삭제**: 오염된 58건(`scores`/`beatmaps`/`replays/*.osr`) 전부 지움. 등록된
  유저(`users`) 행은 유지 — 계정 자체가 잘못된 게 아니라 그 계정이 남의 데이터를 올렸던 것뿐이라
  키를 새로 발급받을 필요는 없음.
- `docs/guides/architecture.md` §22에 이번 필터링 근거 추가.

## 릴리즈 v6.8.1

- `.iss` `MyAppVersion` 6.8.0→6.8.1, `.csproj` `<Version>` →6.8.1.
